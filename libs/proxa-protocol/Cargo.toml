[package]
name = "proxa-protocol"
description = "define the protocol used to send and recieve data between the proxa server, proxa client and game"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3.3"
